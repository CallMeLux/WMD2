using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scr_HotkeyAbility
{
    /*
    public enum ObjectType
    {
        Chair,
        Painting,
        TrophyStand,
        Trophy,
        KelpDollars,
    }


    private BuildSystem playerbuilder;
    private List<HotkeyBuilder> hotkeyBuilderList;

    public scr_HotkeyAbility(ItemSwap player)
    {
        this.playerbuilder = playerbuilder;
        hotkeyBuilderList = new List<HotkeyAbility>();
        hotkeyBuilderList.Add(new HotkeyBuilder {builderType = BuilderType.})
    }

    public void Update()
    {
        if (Input.GetKeyDown(KeyCode.Alpha1))
        {
        playerbuilder.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha2))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha3))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha4))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha5))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha6))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
    }
    public class HotkeyAbility
    {
        public ObjectType objectType;
        public Action activateObjectAction;
    }*/
    /*private scr_BuildSystem playerbuilder;
    public void Update()
    {
        if (Input.GetKeyDown(KeyCode.Alpha1))
        {
        playerbuilder.FloorBuild = new gameObject(scr_itemAssets.kelpDollarsObject);
        playerbuilder.FloorPrefab = new GameObject(scr_itemAssets.kelpDollarsPrefab);
        }
        /*if (Input.GetKeyDown(KeyCode.Alpha2))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha3))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha4))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha5))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }
        if (Input.GetKeyDown(KeyCode.Alpha6))
        {
        player.SetWeaponType(PlayerSwapWeapons.WeaponType.Pistol);
        }*/
}
